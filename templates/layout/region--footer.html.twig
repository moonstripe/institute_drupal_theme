{% if content %}
    <footer class="w-screen flex flex-col space-y-2 mb-16">
        {{ content }}
        <div class="flex w-5/6 md:w-2/3 mx-auto">Institute for Countering Digital Extremism &copy; {{ 'now'|date("Y") }}</div>
    </footer>
{% endif %}
