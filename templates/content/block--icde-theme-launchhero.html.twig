{% if content.body %}
    <div class="relative h-[400px] w-full">
        {% if content.field_hero_image %}{{ content.field_hero_image }}{% endif %}
        <div class="absolute top-0 left-0 flex h-[400px] font-sans font-light w-full">
            <div class="my-auto mx-auto w-4/5 md:w-1/2 text-2xl text-light">{{ content.body }}</div>
        </div>
    </div>
{% endif %}
